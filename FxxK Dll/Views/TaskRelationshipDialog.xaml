<Window x:Class="FxxkDDL.Views.TaskRelationshipDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FxxkDDL.Views"
        mc:Ignorable="d"
        Title="任务关系管理"
        Height="550"
        Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="#F8F9FA">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="0" Opacity="0.2" BlurRadius="4" Color="#2C3E50"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题和当前任务信息 -->
        <Border Grid.Row="0" Background="White" CornerRadius="4" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="任务关系管理" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
                <TextBlock x:Name="TxtTaskInfo" Text="正在加载任务信息..." FontSize="14" Foreground="#7F8C8D" Margin="0,5,0,0"/>
                <TextBlock x:Name="TxtTaskRelations" Text="关系：无" FontSize="12" Foreground="#95A5A6" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- 关系管理区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：父任务和依赖管理 -->
            <StackPanel Grid.Column="0">
                <Border Background="White" CornerRadius="4" Padding="15" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="📋 父任务管理" FontSize="16" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,10"/>

                        <!-- 当前父任务信息 -->
                        <Border Background="#F8F9FA" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="当前父任务：" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                <TextBlock x:Name="TxtCurrentParent" Text="无" FontSize="13" Foreground="#7F8C8D" Margin="0,5,0,0"/>
                                <Button x:Name="BtnRemoveParent" Content="移除父任务"
                                        Background="#E74C3C" Foreground="White"
                                        Margin="0,10,0,0" HorizontalAlignment="Left"
                                        Click="BtnRemoveParent_Click"/>
                            </StackPanel>
                        </Border>

                        <!-- 设置新父任务 -->
                        <StackPanel>
                            <TextBlock Text="设置新父任务：" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                            <ComboBox x:Name="CmbParentTasks" DisplayMemberPath="Description"
                                      Height="30" Margin="0,0,0,10"/>
                            <Button x:Name="BtnSetParent" Content="设为父任务"
                                    Background="#3498DB" Foreground="White"
                                    HorizontalAlignment="Left"
                                    Click="BtnSetParent_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 依赖任务管理 -->
                <Border Background="White" CornerRadius="4" Padding="15" Margin="0,10,0,0">
                    <StackPanel>
                        <TextBlock Text="🔗 依赖任务（前置任务）" FontSize="16" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,10"/>

                        <!-- 当前依赖任务列表 -->
                        <Border Background="#F8F9FA" CornerRadius="4" Padding="10" Margin="0,0,0,10" MaxHeight="150">
                            <StackPanel>
                                <TextBlock Text="当前依赖的任务：" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                <ListView x:Name="LstDependencies"
                                          Height="100"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Margin="0,5,0,0">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Description}" Width="200" TextTrimming="CharacterEllipsis"/>
                                                <Button Content="×"
                                                        Background="#E74C3C" Foreground="White"
                                                        FontSize="10" Padding="6,2" Margin="10,0,0,0"
                                                        Tag="{Binding Id}"
                                                        Click="BtnRemoveDependency_Click"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>
                        </Border>

                        <!-- 添加新依赖 -->
                        <StackPanel>
                            <TextBlock Text="添加新依赖：" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                            <ComboBox x:Name="CmbAvailableTasks" DisplayMemberPath="Description"
                                      Height="30" Margin="0,0,0,10"/>
                            <Button x:Name="BtnAddDependency" Content="添加依赖"
                                    Background="#9B59B6" Foreground="White"
                                    HorizontalAlignment="Left"
                                    Click="BtnAddDependency_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- 分隔线 -->
            <Border Grid.Column="1" Background="#ECF0F1" Width="2" HorizontalAlignment="Center"/>

            <!-- 右侧：子任务管理 -->
            <StackPanel Grid.Column="2">
                <Border Background="White" CornerRadius="4" Padding="15">
                    <StackPanel>
                        <TextBlock Text="👶 子任务管理" FontSize="16" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,10"/>

                        <!-- 当前子任务列表 -->
                        <Border Background="#F8F9FA" CornerRadius="4" Padding="10" Margin="0,0,0,10" MaxHeight="200">
                            <StackPanel>
                                <TextBlock Text="当前子任务：" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                <ListView x:Name="LstSubTasks"
                                          Height="150"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Margin="0,5,0,0">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Description}" Width="200" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text=" (排序: {Binding TaskOrder})" Foreground="#95A5A6" Margin="10,0,0,0"/>
                                                <Button Content="移除"
                                                        Background="#E74C3C" Foreground="White"
                                                        FontSize="10" Padding="6,2" Margin="10,0,0,0"
                                                        Tag="{Binding Id}"
                                                        Click="BtnRemoveSubTask_Click"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>
                        </Border>

                        <!-- 子任务排序 -->
                        <StackPanel>
                            <TextBlock Text="子任务排序：" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                            <TextBlock Text="使用上下按钮调整子任务顺序" FontSize="11" Foreground="#95A5A6" Margin="0,0,0,5"/>
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="BtnMoveUp" Content="↑ 上移"
                                        Background="#3498DB" Foreground="White"
                                        Margin="0,0,5,10" Padding="8,4"
                                        Click="BtnMoveUp_Click"/>
                                <Button x:Name="BtnMoveDown" Content="↓ 下移"
                                        Background="#3498DB" Foreground="White"
                                        Margin="0,0,0,10" Padding="8,4"
                                        Click="BtnMoveDown_Click"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- 添加新子任务 -->
                        <StackPanel>
                            <TextBlock Text="添加新子任务：" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                            <TextBlock Text="选择现有任务作为子任务" FontSize="11" Foreground="#95A5A6" Margin="0,0,0,5"/>
                            <ComboBox x:Name="CmbAvailableForSubTasks" DisplayMemberPath="Description"
                                      Height="30" Margin="0,0,0,10"/>
                            <Button x:Name="BtnAddSubTask" Content="添加为子任务"
                                    Background="#2ECC71" Foreground="White"
                                    HorizontalAlignment="Left"
                                    Click="BtnAddSubTask_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <!-- 底部按钮 -->
        <Border Grid.Row="2" Background="White" CornerRadius="4" Padding="15" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnSave" Content="💾 保存更改"
                        Background="#3498DB" Foreground="White"
                        Width="120" Height="35"
                        Click="BtnSave_Click"/>
                <Button x:Name="BtnClose" Content="❌ 关闭"
                        Background="#95A5A6" Foreground="White"
                        Width="100" Height="35" Margin="10,0,0,0"
                        Click="BtnClose_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>