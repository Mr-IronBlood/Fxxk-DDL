<UserControl x:Class="FxxkDDL.Views.WelcomePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:FxxkDDL.Core.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:WelcomeViewModel/>
    </UserControl.DataContext>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 欢迎内容区域 -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- 应用Logo -->
            <TextBlock Text="📅"
                       FontSize="80"
                       Foreground="#333333"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <!-- 应用名称 -->
            <TextBlock Text="DDL Solver"
                       Foreground="#333333"
                       FontSize="36"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>

            <!-- 欢迎语 -->
            <TextBlock Text="欢迎使用DDL智能管理器"
                       Foreground="#555555"
                       FontSize="18"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"/>

            <!-- 版本信息 -->
            <TextBlock Text="版本 1.0.0"
                       Foreground="#777777"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,40"/>

        </StackPanel>

        <!-- 底部版权信息 -->
        <Border Grid.Row="2" Background="#F5F5F5" Padding="20,10">
            <TextBlock Text="© 2026 DDL Solver 团队 | 智能截止日期管理工具"
                       Foreground="#666666"
                       FontSize="11"
                       HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>