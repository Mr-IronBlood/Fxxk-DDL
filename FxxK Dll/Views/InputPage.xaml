<UserControl x:Class="FxxkDDL.Views.InputPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:FxxkDDL.Core.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:InputViewModel/>
    </UserControl.DataContext>

    <Grid Background="#F8F9FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 - 使用与状态栏一致的浅灰色 -->
        <Border Grid.Row="0" Padding="20,15">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#F5F7FA" Offset="0"/>
                    <GradientStop Color="#ECF0F1" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" Opacity="0.1" BlurRadius="8" Color="#2C3E50"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BtnPaste"
                        Content="📋 粘贴文本"
                        Padding="15,8" Margin="0,0,12,0"
                        Background="#3498DB"
                        Foreground="White"
                        FontWeight="SemiBold"
                        FontSize="13"
                        Cursor="Hand"
                        BorderThickness="0"
                        Command="{Binding PasteCommand}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                            <Border.Effect>
                                                <DropShadowEffect ShadowDepth="0" Opacity="0.2" BlurRadius="4" Color="#2C3E50"/>
                                            </Border.Effect>
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2980B9"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button x:Name="BtnImport"
                        Content="📄 导入文件"
                        Padding="15,8" Margin="0,0,12,0"
                        Background="#2C3E50"
                        Foreground="White"
                        FontWeight="SemiBold"
                        FontSize="13"
                        Cursor="Hand"
                        BorderThickness="0"
                        Command="{Binding ImportCommand}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                            <Border.Effect>
                                                <DropShadowEffect ShadowDepth="0" Opacity="0.2" BlurRadius="4" Color="#2C3E50"/>
                                            </Border.Effect>
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1A252F"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button x:Name="BtnClear"
                        Content="🧹 清空"
                        Padding="15,8"
                        Background="#95A5A6"
                        Foreground="White"
                        FontWeight="SemiBold"
                        FontSize="13"
                        Cursor="Hand"
                        BorderThickness="0"
                        Command="{Binding ClearCommand}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                            <Border.Effect>
                                                <DropShadowEffect ShadowDepth="0" Opacity="0.2" BlurRadius="4" Color="#2C3E50"/>
                                            </Border.Effect>
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#7F8C8D"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- 字符计数显示 -->
                <TextBlock x:Name="TxtCharCount"
                           Text="{Binding CharacterCount, StringFormat='{}{0} 字符'}"
                           VerticalAlignment="Center"
                           Margin="25,0,0,0"
                           FontSize="13"
                           Foreground="#7F8C8D"/>
            </StackPanel>
        </Border>

        <!-- 主内容区域 - 白色背景，与主界面内容区一致 -->
        <Border Grid.Row="1" Margin="20,15,20,15"
                CornerRadius="8"
                BorderBrush="#E0E6ED"
                BorderThickness="1">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#FFFFFF" Offset="0"/>
                    <GradientStop Color="#F8F9FA" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" Opacity="0.15" BlurRadius="12" Color="#2C3E50"/>
            </Border.Effect>

            <Grid>
                <!-- 文本输入区域 -->
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBox x:Name="TxtInput"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             FontSize="14"
                             Foreground="#2C3E50"
                             VerticalAlignment="Stretch"
                             Text="{Binding ChatText, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Background" Value="#FFFFFF"/>
                                <Setter Property="BorderBrush" Value="#D5DBDB"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="Padding" Value="20"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Grid>
                                                <Border x:Name="border"
                                                        Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="4">
                                                    <ScrollViewer x:Name="PART_ContentHost"
                                                                  Margin="{TemplateBinding Padding}"/>
                                                </Border>
                                                <!-- 水印文本 -->
                                                <TextBlock x:Name="watermark"
                                                           Text="请在此粘贴或输入聊天记录..."
                                                           Foreground="#95A5A6"
                                                           FontStyle="Italic"
                                                           Margin="{TemplateBinding Padding}"
                                                           Padding="2"
                                                           VerticalAlignment="Top"
                                                           IsHitTestVisible="False"
                                                           Visibility="Collapsed"/>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsFocused" Value="True">
                                                    <Setter TargetName="border" Property="BorderBrush" Value="#3498DB"/>
                                                    <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                                </Trigger>
                                                <!-- 当文本为空且未聚焦时显示水印 -->
                                                <MultiTrigger>
                                                    <MultiTrigger.Conditions>
                                                        <Condition Property="Text" Value=""/>
                                                        <Condition Property="IsFocused" Value="False"/>
                                                    </MultiTrigger.Conditions>
                                                    <Setter TargetName="watermark" Property="Visibility" Value="Visible"/>
                                                </MultiTrigger>
                                                <MultiTrigger>
                                                    <MultiTrigger.Conditions>
                                                        <Condition Property="Text" Value="{x:Null}"/>
                                                        <Condition Property="IsFocused" Value="False"/>
                                                    </MultiTrigger.Conditions>
                                                    <Setter TargetName="watermark" Property="Visibility" Value="Visible"/>
                                                </MultiTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                        <!-- 文本框的上下文菜单 -->
                        <TextBox.ContextMenu>
                            <ContextMenu Background="White" BorderBrush="#D5DBDB">
                                <MenuItem Header="粘贴" Command="{Binding PasteCommand}"/>
                                <MenuItem Header="剪切" InputGestureText="Ctrl+X"/>
                                <MenuItem Header="复制" InputGestureText="Ctrl+C"/>
                                <Separator/>
                                <MenuItem Header="全选" InputGestureText="Ctrl+A"/>
                            </ContextMenu>
                        </TextBox.ContextMenu>
                    </TextBox>
                </ScrollViewer>

                <!-- 右下角字符提示 -->
                <Border HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Margin="10"
                        Background="#F8F9FA"
                        Padding="10,6"
                        CornerRadius="4"
                        BorderBrush="#D5DBDB"
                        BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="0" Opacity="0.1" BlurRadius="4" Color="#7F8C8D"/>
                    </Border.Effect>
                    <TextBlock Text="支持直接粘贴、TXT文本（目前仅支持文本，其他文件格式暂不支持）"
                               FontSize="11"
                               Foreground="#5D6D7E"/>
                </Border>
            </Grid>
        </Border>

        <!-- 底部操作栏 - 与工具栏一致的浅灰色 -->
        <Border Grid.Row="2" Padding="20,15">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#F5F7FA" Offset="0"/>
                    <GradientStop Color="#ECF0F1" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" Opacity="0.1" BlurRadius="8" Color="#2C3E50"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 左侧提示信息 -->
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="智能DDL提取"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="#2C3E50"/>
                    <TextBlock Text="使用DeepSeek AI自动分析聊天记录中的截止日期（目前仅支持文本格式）"
                               FontSize="11"
                               Foreground="#7F8C8D"/>

                    <!-- 文件信息显示 -->
                    <StackPanel Margin="0,8,0,0"
                                Visibility="{Binding HasSelectedFile, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Border Background="#E8F4FD"
                                CornerRadius="6"
                                Padding="10,8"
                                BorderBrush="#3498DB"
                                BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="0" Opacity="0.15" BlurRadius="6" Color="#3498DB"/>
                            </Border.Effect>
                            <StackPanel>
                                <TextBlock Text="{Binding SelectedFileName}"
                                           FontSize="11"
                                           FontWeight="SemiBold"
                                           Foreground="#2C3E50"
                                           TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding FileUploadStatus}"
                                           FontSize="10"
                                           Foreground="#3498DB"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>

                <!-- 右侧分析按钮和状态 -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <!-- 分析状态提示 -->
                    <TextBlock Text="{Binding FileUploadStatus}"
                               FontSize="11"
                               Foreground="#E74C3C"
                               Margin="0,0,0,8"
                               TextWrapping="Wrap"
                               HorizontalAlignment="Right">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding FileUploadStatus}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding FileUploadStatus}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding FileUploadStatus}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- 分析按钮 -->
                    <Button x:Name="BtnAnalyze"
                            Content="{Binding AnalyzeButtonText}"
                            Padding="25,12"
                            Background="#3498DB"
                            Foreground="White"
                            FontSize="14"
                            FontWeight="Bold"
                            Cursor="Hand"
                            BorderThickness="0"
                            Command="{Binding AnalyzeCommand}"
                            IsEnabled="{Binding IsAnalyzing, Converter={StaticResource InverseBoolConverter}}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4">
                                                <Border.Effect>
                                                    <DropShadowEffect ShadowDepth="0" Opacity="0.2" BlurRadius="4" Color="#2C3E50"/>
                                                </Border.Effect>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    <TextBlock Text=" (分析中...)" Visibility="{Binding IsAnalyzing, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                </StackPanel>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#2980B9"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Background" Value="#95A5A6"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>