<UserControl x:Class="FxxkDDL.Views.CalendarPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FxxkDDL.Views"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <!-- 转换器 -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- 卡片阴影效果 -->
        <DropShadowEffect x:Key="CardShadow" Color="Black" Opacity="0.08" BlurRadius="6" ShadowDepth="2"/>
        <DropShadowEffect x:Key="CardShadowHover" Color="Black" Opacity="0.15" BlurRadius="8" ShadowDepth="3"/>
    </UserControl.Resources>

    <Grid Background="#F8F9FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 日历控制栏 -->
        <Border Grid.Row="0" Background="White" 
                BorderBrush="#D5DBDB" BorderThickness="0,0,0,1"
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 视图切换 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button x:Name="BtnMonthView" Content="📅 月视图"
                                Padding="12,8" Margin="0,0,10,0"
                                Background="#3498DB" Foreground="White"
                                FontWeight="SemiBold" FontSize="13"
                                Cursor="Hand"
                                Click="BtnMonthView_Click"/>

                    <Button x:Name="BtnWeekView" Content="📆 周视图"
                                Padding="12,8"
                                Background="Transparent" Foreground="#2C3E50"
                                FontWeight="SemiBold" FontSize="13"
                                BorderBrush="#D5DBDB" BorderThickness="1"
                                Cursor="Hand"
                                Click="BtnWeekView_Click"/>
                </StackPanel>

                <!-- 当前月份/周显示 -->
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock x:Name="TxtCurrentPeriod" 
                              Text="2026年01月"
                              FontSize="18" FontWeight="Bold"
                              Foreground="#2C3E50"
                              HorizontalAlignment="Center"/>

                    <TextBlock Text="点击任务查看详情和管理"
                              FontSize="12"
                              Foreground="#7F8C8D"
                              HorizontalAlignment="Center"
                              Margin="0,5,0,0"/>
                </StackPanel>

                <!-- 导航按钮 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="BtnPrev" Content="◀ 上个月"
                                Padding="10,8" Margin="0,0,10,0"
                                Background="Transparent" Foreground="#2C3E50"
                                BorderBrush="#D5DBDB" BorderThickness="1"
                                Cursor="Hand"
                                Click="BtnPrev_Click"/>

                    <Button x:Name="BtnToday" Content="今天"
                                Padding="10,8" Margin="0,0,10,0"
                                Background="#3498DB" Foreground="White"
                                FontWeight="SemiBold" Cursor="Hand"
                                Click="BtnToday_Click"/>

                    <Button x:Name="BtnNext" Content="下个月 ▶"
                                Padding="10,8"
                                Background="Transparent" Foreground="#2C3E50"
                                BorderBrush="#D5DBDB" BorderThickness="1"
                                Cursor="Hand"
                                Click="BtnNext_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 日历内容区域 -->
        <Border Grid.Row="1" Margin="20,0,20,20">
            <Grid x:Name="CalendarGrid">
                <!-- 月视图容器 -->
                <Border x:Name="MonthViewContainer" 
                        Background="White"
                        CornerRadius="8"
                        BorderBrush="#D5DBDB" BorderThickness="1"
                        Padding="15">

                    <Grid>
                        <!-- 星期标题 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="日" HorizontalAlignment="Center" 
                                      FontWeight="Bold" Foreground="#E74C3C" Padding="10"/>
                            <TextBlock Grid.Column="1" Text="一" HorizontalAlignment="Center" 
                                      FontWeight="Bold" Foreground="#2C3E50" Padding="10"/>
                            <TextBlock Grid.Column="2" Text="二" HorizontalAlignment="Center" 
                                      FontWeight="Bold" Foreground="#2C3E50" Padding="10"/>
                            <TextBlock Grid.Column="3" Text="三" HorizontalAlignment="Center" 
                                      FontWeight="Bold" Foreground="#2C3E50" Padding="10"/>
                            <TextBlock Grid.Column="4" Text="四" HorizontalAlignment="Center" 
                                      FontWeight="Bold" Foreground="#2C3E50" Padding="10"/>
                            <TextBlock Grid.Column="5" Text="五" HorizontalAlignment="Center" 
                                      FontWeight="Bold" Foreground="#2C3E50" Padding="10"/>
                            <TextBlock Grid.Column="6" Text="六" HorizontalAlignment="Center" 
                                      FontWeight="Bold" Foreground="#3498DB" Padding="10"/>
                        </Grid>

                        <!-- 日期网格 -->
                        <ItemsControl Grid.Row="1" x:Name="MonthDaysControl">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="7" Rows="6"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E5E7EB" BorderThickness="0,0,1,1"
                                                Width="90" Height="95"
                                                Background="White"
                                                Cursor="Hand"
                                                MouseEnter="DayCell_MouseEnter"
                                                MouseLeave="DayCell_MouseLeave"
                                                MouseLeftButtonUp="DayCell_MouseLeftButtonUp"
                                                Effect="{StaticResource CardShadow}">

                                        <Grid>
                                            <!-- 内容区域 -->
                                            <Grid Margin="0,8,0,0">
                                                <!-- 日期数字 -->
                                                <TextBlock Text="{Binding DayNumber}"
                                                          FontSize="16" FontWeight="SemiBold"
                                                          HorizontalAlignment="Left"
                                                          VerticalAlignment="Top"
                                                          Margin="8,0,0,0"
                                                          Foreground="{Binding TextColor}"/>

                                                <!-- DDL标记点区域 -->
                                                <ItemsControl ItemsSource="{Binding Dots}"
                                                              HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"
                                                              Margin="0,5,0,0">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel Orientation="Horizontal" ItemWidth="10"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>

                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Ellipse Width="6" Height="6"
                                                                         Fill="{Binding Color}"
                                                                         Margin="0,0,2,2"/>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>

                                                <!-- 事件计数 -->
                                                <Border HorizontalAlignment="Center"
                                                        VerticalAlignment="Bottom"
                                                        Margin="0,0,0,8"
                                                        Padding="6,2,6,2"
                                                        Background="#F8F9FA"
                                                        CornerRadius="10"
                                                        Visibility="{Binding HasEvents, Converter={StaticResource BoolToVisibilityConverter}}">
                                                    <TextBlock Text="{Binding EventCount, StringFormat='{}{0}'}"
                                                              FontSize="10" FontWeight="Medium"
                                                              Foreground="#7F8C8D"/>
                                                </Border>
                                            </Grid>

                                            <!-- 如果是今天，显示蓝色背景圆 -->
                                            <Border Background="#3498DB"
                                                    CornerRadius="12"
                                                    Width="28" Height="28"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Top"
                                                    Margin="6,6,0,0"
                                                    IsHitTestVisible="False"
                                                    Visibility="{Binding IsToday, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <TextBlock Text="{Binding DayNumber}"
                                                          FontSize="16" FontWeight="Bold"
                                                          Foreground="White"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>


                <!-- 周视图容器 -->
                <Border x:Name="WeekViewContainer"
                        Background="White"
                        CornerRadius="8"
                        BorderBrush="#D5DBDB" BorderThickness="1"
                        Padding="15"
                        Visibility="Collapsed">

                    <Grid MinHeight="600">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- 周导航按钮 -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                            <Button x:Name="BtnWeekPrev" Content="◀ 上周"
                                    Padding="12,6" Margin="0,0,10,0"
                                    Background="#F8F9FA" Foreground="#2C3E50"
                                    BorderBrush="#D5DBDB" BorderThickness="1"
                                    Cursor="Hand"
                                    Click="BtnWeekPrev_Click"/>

                            <Button x:Name="BtnWeekViewToggle" Content="切换14天视图"
                                    Padding="12,6" Margin="10,0,10,0"
                                    Background="#3498DB" Foreground="White"
                                    BorderBrush="#2980B9" BorderThickness="1"
                                    Cursor="Hand"
                                    Click="BtnWeekViewToggle_Click"/>

                            <Button x:Name="BtnWeekNext" Content="下周 ▶"
                                    Padding="12,6" Margin="0,0,0,0"
                                    Background="#F8F9FA" Foreground="#2C3E50"
                                    BorderBrush="#D5DBDB" BorderThickness="1"
                                    Cursor="Hand"
                                    Click="BtnWeekNext_Click"/>
                        </StackPanel>

                        <!-- 周视图主内容区域 -->
                        <Grid x:Name="WeekViewMainGrid" Grid.Row="1">
                            <!-- 日期行 -->
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>   <!-- 日期头部 -->
                                <RowDefinition Height="*"/>    <!-- 任务显示区域 -->
                            </Grid.RowDefinitions>

                            <!-- 日期头部栏（可滚动） -->
                            <Border Grid.Row="0" Background="#F8F9FA" CornerRadius="8,8,0,0" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" HorizontalAlignment="Left">
                                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" x:Name="HeaderScrollViewer" HorizontalAlignment="Left">
                                    <ItemsControl x:Name="WeekHeaderControl" HorizontalAlignment="Left">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Width="120" Padding="8,12"
                                                        Background="{Binding DateBackground}"
                                                        BorderBrush="#E5E7EB"
                                                        BorderThickness="0,0,1,0">
                                                    <StackPanel HorizontalAlignment="Center">
                                                        <TextBlock Text="{Binding DayOfWeek}"
                                                                   FontSize="12" FontWeight="Bold"
                                                                   Foreground="{Binding DayOfWeekColor}"
                                                                   HorizontalAlignment="Center"/>
                                                        <TextBlock Text="{Binding DateString}"
                                                                   FontSize="16" FontWeight="Bold"
                                                                   Foreground="{Binding DateColor}"
                                                                   HorizontalAlignment="Center"
                                                                   Margin="0,2,0,0"/>
                                                        <Border Background="#3498DB"
                                                                CornerRadius="10"
                                                                Padding="6,2"
                                                                Visibility="{Binding IsToday, Converter={StaticResource BoolToVisibilityConverter}}"
                                                                Margin="0,2,0,0">
                                                            <TextBlock Text="今天"
                                                                       FontSize="10"
                                                                       Foreground="White"
                                                                       FontWeight="Bold"
                                                                       HorizontalAlignment="Center"/>
                                                        </Border>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Border>

                            <!-- 任务拖尾显示区域（可滚动，与日期头部同步） -->
                            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" x:Name="TaskScrollViewer" HorizontalAlignment="Left" VerticalAlignment="Top" MinHeight="540">
                                <Grid x:Name="TaskTrailGrid" Background="White" HorizontalAlignment="Left" VerticalAlignment="Top" MinHeight="540">
                                    <!-- 任务拖尾Canvas层 -->
                                    <Canvas x:Name="TaskTrailCanvas" />

                                    <!-- 任务交互层 -->
                                    <Canvas x:Name="TaskInteractionCanvas" />
                                </Grid>
                            </ScrollViewer>
                        </Grid>
                    </Grid>
                </Border>
        </Grid>
    </Border>
</Grid>
</UserControl>
