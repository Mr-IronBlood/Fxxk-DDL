<Window x:Class="FxxkDDL.Views.AddTaskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FxxkDDL.Views"
        mc:Ignorable="d"
        Title="手动添加任务"
        Height="580"
        Width="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="#F8F9FA"
        FontFamily="Segoe UI, Microsoft YaHei">

    <Window.Resources>
        <!-- 文本样式 -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei"/>
        </Style>

        <!-- 输入框样式 -->
        <Style x:Key="InputBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#3498DB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 下拉框样式 -->
        <Style x:Key="InputComboStyle" TargetType="ComboBox">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="32"/>
        </Style>

        <!-- 按钮样式 -->
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                Margin="{TemplateBinding Margin}">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="0" Opacity="0.2" BlurRadius="4" Color="#2C3E50"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题区域 -->
        <Border Grid.Row="0" Background="White" CornerRadius="4" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="➕ 手动添加任务" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
                <TextBlock Text="填写以下信息创建新任务（带*为必填项）" FontSize="12" Foreground="#95A5A6" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- 内容区域 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Border Background="White" CornerRadius="4" Padding="20">
                <StackPanel>
                    <!-- 任务名称 -->
                    <TextBlock Text="任务名称 *" FontSize="14" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,8"/>
                    <TextBox x:Name="TxtTaskName"
                             Style="{StaticResource InputBoxStyle}"
                             Height="32"
                             MaxLength="50"
                             Margin="0,0,0,15"/>

                    <!-- 任务详情 -->
                    <TextBlock Text="任务详情 *" FontSize="14" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,8"/>
                    <TextBox x:Name="TxtTaskDetail"
                             Style="{StaticResource InputBoxStyle}"
                             Height="90"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Margin="0,0,0,15"/>

                    <!-- 原文内容 -->
                    <TextBlock Text="原文内容（可选）" FontSize="14" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,8"/>
                    <TextBox x:Name="TxtOriginalText"
                             Style="{StaticResource InputBoxStyle}"
                             Height="60"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Margin="0,0,0,15"/>

                    <!-- 截止时间 -->
                    <TextBlock Text="截止时间" FontSize="14" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,8"/>
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="日期:" FontSize="13" Foreground="#2C3E50" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <DatePicker x:Name="DpDeadline"
                                    Grid.Column="1"
                                    SelectedDate="{x:Static local:AddTaskDialog.TodayPlusSeven}"
                                    Height="32"
                                    Padding="8,4"
                                    FontSize="13"
                                    BorderBrush="#BDC3C7"
                                    BorderThickness="1"/>

                        <TextBlock Grid.Column="3" Text="时间:" FontSize="13" Foreground="#2C3E50" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox x:Name="CmbTime"
                                   Grid.Column="4"
                                   SelectedIndex="47"
                                   Style="{StaticResource InputComboStyle}"/>
                    </Grid>

                    <!-- 重要性 -->
                    <TextBlock Text="重要性" FontSize="14" FontWeight="SemiBold" Foreground="#2C3E50" Margin="0,0,0,8"/>
                    <ComboBox x:Name="CmbImportance"
                              Style="{StaticResource InputComboStyle}"
                              Width="100"
                              HorizontalAlignment="Left"
                              Margin="0,0,0,10"/>
                </StackPanel>
            </Border>
        </ScrollViewer>

        <!-- 底部按钮 -->
        <Border Grid.Row="2" Background="White" CornerRadius="4" Padding="15" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnSave"
                        Content="💾 保存任务"
                        Background="#2ECC71"
                        Foreground="White"
                        Width="110"
                        Click="BtnSave_Click"/>
                <Button x:Name="BtnCancel"
                        Content="❌ 取消"
                        Background="#95A5A6"
                        Foreground="White"
                        Width="90"
                        Click="BtnCancel_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
