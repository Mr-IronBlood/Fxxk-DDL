<UserControl x:Class="FxxkDDL.Views.TasksPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:FxxkDDL.Core.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.DataContext>
        <vm:TasksViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <!-- 任务卡片样式 -->
        <Style x:Key="TaskCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#ECF0F1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.05" BlurRadius="6" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#3498DB"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="8" ShadowDepth="3"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 重要性颜色标记 -->
        <DataTemplate x:Key="ImportanceBadgeTemplate">
            <Border Background="{Binding ImportanceColor}" CornerRadius="12" Padding="10,4">
                <TextBlock Text="{Binding Importance}" FontSize="11" FontWeight="Bold" Foreground="White"/>
            </Border>
        </DataTemplate>

        <!-- FAB 浮动按钮样式 -->
        <Style x:Key="FABButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Width" Value="42"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <!-- 多层阴影增强立体感 -->
                            <Ellipse Fill="{TemplateBinding Background}" Width="42" Height="42">
                                <Ellipse.Effect>
                                    <DropShadowEffect Color="Black" Opacity="0.5" BlurRadius="12" ShadowDepth="6" Direction="270"/>
                                </Ellipse.Effect>
                            </Ellipse>
                            <!-- 内层阴影增强层次 -->
                            <Ellipse Stroke="#1A5276" StrokeThickness="2" Width="42" Height="42" Opacity="0.4"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-1,0,0"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#21618C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <!-- 外层 Grid 用于承载 FAB 按钮 -->
    <Grid Background="#F8F9FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <Border Grid.Row="0" Background="#ECF0F1" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="📋 全部任务"
                            Padding="12,8" Margin="0,0,10,0"
                            FontWeight="SemiBold" FontSize="13"
                            Command="{Binding ShowAllCommand}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#2C3E50"/>
                                <Setter Property="BorderBrush" Value="#D5DBDB"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentFilter}" Value="all">
                                        <Setter Property="Background" Value="#3498DB"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="⏳ 待完成"
                            Padding="12,8" Margin="0,0,10,0"
                            FontSize="13"
                            Command="{Binding ShowPendingCommand}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#2C3E50"/>
                                <Setter Property="BorderBrush" Value="#D5DBDB"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentFilter}" Value="pending">
                                        <Setter Property="Background" Value="#3498DB"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="✅ 已完成"
                            Padding="12,8"
                            FontSize="13"
                            Command="{Binding ShowCompletedCommand}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#2C3E50"/>
                                <Setter Property="BorderBrush" Value="#D5DBDB"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentFilter}" Value="completed">
                                        <Setter Property="Background" Value="#3498DB"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Separator Width="20" Background="Transparent"/>

                    <Button Content="🗑️ 清空已完成"
                            Padding="12,8"
                            Background="#E74C3C" Foreground="White"
                            FontSize="13"
                            Command="{Binding DeleteCompletedTasksCommand}"/>
                </StackPanel>

                <TextBlock Grid.Column="1"
                           Text="{Binding TaskCountText}"
                           VerticalAlignment="Center"
                           FontSize="13"
                           Foreground="#7F8C8D"/>
            </Grid>
        </Border>

        <!-- 任务统计栏 -->
        <Border Grid.Row="1" Background="White" Padding="15" Margin="20,10,20,10" CornerRadius="6">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="20,0">
                    <Border Background="#E74C3C" CornerRadius="15" Width="40" Height="40" VerticalAlignment="Center">
                        <TextBlock Text="{Binding HighTasksCount}" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center" Margin="10,0,0,0">
                        <TextBlock Text="高优先级" FontSize="12" Foreground="#7F8C8D"/>
                        <TextBlock Text="{Binding HighTasksText}" FontSize="14" FontWeight="Bold" Foreground="#2C3E50"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="20,0">
                    <Border Background="#F1C40F" CornerRadius="15" Width="40" Height="40" VerticalAlignment="Center">
                        <TextBlock Text="{Binding MediumTasksCount}" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center" Margin="10,0,0,0">
                        <TextBlock Text="中优先级" FontSize="12" Foreground="#7F8C8D"/>
                        <TextBlock Text="{Binding MediumTasksText}" FontSize="14" FontWeight="Bold" Foreground="#2C3E50"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="20,0">
                    <Border Background="#2ECC71" CornerRadius="15" Width="40" Height="40" VerticalAlignment="Center">
                        <TextBlock Text="{Binding LowTasksCount}" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center" Margin="10,0,0,0">
                        <TextBlock Text="低优先级" FontSize="12" Foreground="#7F8C8D"/>
                        <TextBlock Text="{Binding LowTasksText}" FontSize="14" FontWeight="Bold" Foreground="#2C3E50"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="20,0">
                    <Border Background="#95A5A6" CornerRadius="15" Width="40" Height="40" VerticalAlignment="Center">
                        <TextBlock Text="{Binding NoDeadlineTasksCount}" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center" Margin="10,0,0,0">
                        <TextBlock Text="未指定日期" FontSize="12" Foreground="#7F8C8D"/>
                        <TextBlock Text="{Binding NoDeadlineTasksText}" FontSize="14" FontWeight="Bold" Foreground="#2C3E50"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- 任务卡片区域 - 4栏布局 -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="20,0,20,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 高优先级列 -->
                <Border Grid.Column="0" Margin="0,0,10,0">
                    <StackPanel>
                        <Border Background="#E74C3C" CornerRadius="6" Padding="15,10" Margin="0,0,0,15">
                            <Grid>
                                <TextBlock Text="🔴 高优先级" FontSize="16" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="{Binding HighTasksCount, StringFormat='{}{0} 项'}" FontSize="12" Foreground="White" HorizontalAlignment="Right" Opacity="0.9"/>
                            </Grid>
                        </Border>

                        <ItemsControl ItemsSource="{Binding HighPriorityTasks}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource TaskCardStyle}"
                                            Cursor="Hand"
                                            MouseLeftButtonUp="TaskCard_MouseLeftButtonUp"
                                            Tag="{Binding Id}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <!-- 完成状态和描述 -->
                                            <Grid Grid.Row="0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <CheckBox Grid.Column="0"
                                                          IsChecked="{Binding IsCompleted, Mode=TwoWay}"
                                                          Margin="0,2,10,0"
                                                          VerticalAlignment="Top"
                                                          Tag="{Binding Id}"
                                                          Checked="TaskCheckBox_Checked"
                                                          Unchecked="TaskCheckBox_Unchecked"/>

                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding Description}"
                                                           FontSize="14"
                                                           Foreground="#2C3E50"
                                                           TextWrapping="Wrap"
                                                           MaxHeight="60"
                                                           TextTrimming="CharacterEllipsis"
                                                           VerticalAlignment="Top"/>
                                            </Grid>

                                            <!-- 截止时间 -->
                                            <TextBlock Grid.Row="1"
                                                       Text="{Binding DeadlineString}"
                                                       FontSize="11"
                                                       Foreground="#7F8C8D"
                                                       Margin="0,8,0,0"
                                                       Visibility="{Binding HasDeadline, Converter={StaticResource BoolToVisibilityConverter}}"/>

                                            <!-- 关系信息和重要性标签 -->
                                            <Grid Grid.Row="2" Margin="0,8,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding RelationshipInfo}"
                                                           FontSize="10"
                                                           Foreground="#95A5A6"
                                                           TextTrimming="CharacterEllipsis"/>

                                                <Border Grid.Column="1"
                                                        Background="{Binding ImportanceColor}"
                                                        CornerRadius="10"
                                                        Padding="8,3"
                                                        Margin="8,0,0,0">
                                                    <TextBlock Text="{Binding Importance}"
                                                               FontSize="10"
                                                               FontWeight="Bold"
                                                               Foreground="White"/>
                                                </Border>
                                            </Grid>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>

                            <!-- 无任务提示 -->
                            <ItemsControl.Style>
                                <Style TargetType="ItemsControl">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HighPriorityTasks.Count}" Value="0">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate>
                                                        <Border Background="White" CornerRadius="8" Padding="20" BorderBrush="#ECF0F1" BorderThickness="1">
                                                            <TextBlock Text="暂无任务" FontSize="13" Foreground="#95A5A6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ItemsControl.Style>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- 中优先级列 -->
                <Border Grid.Column="1" Margin="10,0">
                    <StackPanel>
                        <Border Background="#F1C40F" CornerRadius="6" Padding="15,10" Margin="0,0,0,15">
                            <Grid>
                                <TextBlock Text="🟡 中优先级" FontSize="16" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="{Binding MediumTasksCount, StringFormat='{}{0} 项'}" FontSize="12" Foreground="White" HorizontalAlignment="Right" Opacity="0.9"/>
                            </Grid>
                        </Border>

                        <ItemsControl ItemsSource="{Binding MediumPriorityTasks}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource TaskCardStyle}"
                                            Cursor="Hand"
                                            MouseLeftButtonUp="TaskCard_MouseLeftButtonUp"
                                            Tag="{Binding Id}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <!-- 完成状态和描述 -->
                                            <Grid Grid.Row="0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <CheckBox Grid.Column="0"
                                                          IsChecked="{Binding IsCompleted, Mode=TwoWay}"
                                                          Margin="0,2,10,0"
                                                          VerticalAlignment="Top"
                                                          Tag="{Binding Id}"
                                                          Checked="TaskCheckBox_Checked"
                                                          Unchecked="TaskCheckBox_Unchecked"/>

                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding Description}"
                                                           FontSize="14"
                                                           Foreground="#2C3E50"
                                                           TextWrapping="Wrap"
                                                           MaxHeight="60"
                                                           TextTrimming="CharacterEllipsis"
                                                           VerticalAlignment="Top"/>
                                            </Grid>

                                            <!-- 截止时间 -->
                                            <TextBlock Grid.Row="1"
                                                       Text="{Binding DeadlineString}"
                                                       FontSize="11"
                                                       Foreground="#7F8C8D"
                                                       Margin="0,8,0,0"
                                                       Visibility="{Binding HasDeadline, Converter={StaticResource BoolToVisibilityConverter}}"/>

                                            <!-- 关系信息和重要性标签 -->
                                            <Grid Grid.Row="2" Margin="0,8,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding RelationshipInfo}"
                                                           FontSize="10"
                                                           Foreground="#95A5A6"
                                                           TextTrimming="CharacterEllipsis"/>

                                                <Border Grid.Column="1"
                                                        Background="{Binding ImportanceColor}"
                                                        CornerRadius="10"
                                                        Padding="8,3"
                                                        Margin="8,0,0,0">
                                                    <TextBlock Text="{Binding Importance}"
                                                               FontSize="10"
                                                               FontWeight="Bold"
                                                               Foreground="White"/>
                                                </Border>
                                            </Grid>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>

                            <!-- 无任务提示 -->
                            <ItemsControl.Style>
                                <Style TargetType="ItemsControl">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding MediumPriorityTasks.Count}" Value="0">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate>
                                                        <Border Background="White" CornerRadius="8" Padding="20" BorderBrush="#ECF0F1" BorderThickness="1">
                                                            <TextBlock Text="暂无任务" FontSize="13" Foreground="#95A5A6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ItemsControl.Style>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- 低优先级列 -->
                <Border Grid.Column="2" Margin="10,0">
                    <StackPanel>
                        <Border Background="#2ECC71" CornerRadius="6" Padding="15,10" Margin="0,0,0,15">
                            <Grid>
                                <TextBlock Text="🟢 低优先级" FontSize="16" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="{Binding LowTasksCount, StringFormat='{}{0} 项'}" FontSize="12" Foreground="White" HorizontalAlignment="Right" Opacity="0.9"/>
                            </Grid>
                        </Border>

                        <ItemsControl ItemsSource="{Binding LowPriorityTasks}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource TaskCardStyle}"
                                            Cursor="Hand"
                                            MouseLeftButtonUp="TaskCard_MouseLeftButtonUp"
                                            Tag="{Binding Id}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <!-- 完成状态和描述 -->
                                            <Grid Grid.Row="0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <CheckBox Grid.Column="0"
                                                          IsChecked="{Binding IsCompleted, Mode=TwoWay}"
                                                          Margin="0,2,10,0"
                                                          VerticalAlignment="Top"
                                                          Tag="{Binding Id}"
                                                          Checked="TaskCheckBox_Checked"
                                                          Unchecked="TaskCheckBox_Unchecked"/>

                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding Description}"
                                                           FontSize="14"
                                                           Foreground="#2C3E50"
                                                           TextWrapping="Wrap"
                                                           MaxHeight="60"
                                                           TextTrimming="CharacterEllipsis"
                                                           VerticalAlignment="Top"/>
                                            </Grid>

                                            <!-- 截止时间 -->
                                            <TextBlock Grid.Row="1"
                                                       Text="{Binding DeadlineString}"
                                                       FontSize="11"
                                                       Foreground="#7F8C8D"
                                                       Margin="0,8,0,0"
                                                       Visibility="{Binding HasDeadline, Converter={StaticResource BoolToVisibilityConverter}}"/>

                                            <!-- 关系信息和重要性标签 -->
                                            <Grid Grid.Row="2" Margin="0,8,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding RelationshipInfo}"
                                                           FontSize="10"
                                                           Foreground="#95A5A6"
                                                           TextTrimming="CharacterEllipsis"/>

                                                <Border Grid.Column="1"
                                                        Background="{Binding ImportanceColor}"
                                                        CornerRadius="10"
                                                        Padding="8,3"
                                                        Margin="8,0,0,0">
                                                    <TextBlock Text="{Binding Importance}"
                                                               FontSize="10"
                                                               FontWeight="Bold"
                                                               Foreground="White"/>
                                                </Border>
                                            </Grid>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>

                            <!-- 无任务提示 -->
                            <ItemsControl.Style>
                                <Style TargetType="ItemsControl">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding LowPriorityTasks.Count}" Value="0">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate>
                                                        <Border Background="White" CornerRadius="8" Padding="20" BorderBrush="#ECF0F1" BorderThickness="1">
                                                            <TextBlock Text="暂无任务" FontSize="13" Foreground="#95A5A6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ItemsControl.Style>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- 未指定日期列 -->
                <Border Grid.Column="3" Margin="10,0,0,0">
                    <StackPanel>
                        <Border Background="#95A5A6" CornerRadius="6" Padding="15,10" Margin="0,0,0,15">
                            <Grid>
                                <TextBlock Text="⚪ 未指定日期" FontSize="16" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="{Binding NoDeadlineTasksCount, StringFormat='{}{0} 项'}" FontSize="12" Foreground="White" HorizontalAlignment="Right" Opacity="0.9"/>
                            </Grid>
                        </Border>

                        <ItemsControl ItemsSource="{Binding NoDeadlineTasks}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource TaskCardStyle}"
                                            Cursor="Hand"
                                            MouseLeftButtonUp="TaskCard_MouseLeftButtonUp"
                                            Tag="{Binding Id}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <!-- 完成状态和描述 -->
                                            <Grid Grid.Row="0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <CheckBox Grid.Column="0"
                                                          IsChecked="{Binding IsCompleted, Mode=TwoWay}"
                                                          Margin="0,2,10,0"
                                                          VerticalAlignment="Top"
                                                          Tag="{Binding Id}"
                                                          Checked="TaskCheckBox_Checked"
                                                          Unchecked="TaskCheckBox_Unchecked"/>

                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding Description}"
                                                           FontSize="14"
                                                           Foreground="#2C3E50"
                                                           TextWrapping="Wrap"
                                                           MaxHeight="60"
                                                           TextTrimming="CharacterEllipsis"
                                                           VerticalAlignment="Top"/>
                                            </Grid>

                                            <!-- 关系信息和重要性标签 -->
                                            <Grid Grid.Row="1" Margin="0,8,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding RelationshipInfo}"
                                                           FontSize="10"
                                                           Foreground="#95A5A6"
                                                           TextTrimming="CharacterEllipsis"/>

                                                <Border Grid.Column="1"
                                                        Background="{Binding ImportanceColor}"
                                                        CornerRadius="10"
                                                        Padding="8,3"
                                                        Margin="8,0,0,0">
                                                    <TextBlock Text="{Binding Importance}"
                                                               FontSize="10"
                                                               FontWeight="Bold"
                                                               Foreground="White"/>
                                                </Border>
                                            </Grid>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>

                            <!-- 无任务提示 -->
                            <ItemsControl.Style>
                                <Style TargetType="ItemsControl">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding NoDeadlineTasks.Count}" Value="0">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate>
                                                        <Border Background="White" CornerRadius="8" Padding="20" BorderBrush="#ECF0F1" BorderThickness="1">
                                                            <TextBlock Text="暂无任务" FontSize="13" Foreground="#95A5A6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ItemsControl.Style>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- FAB 浮动添加按钮 - 放在外层 Grid 内，浮动在整个页面右下角 -->
        <Button x:Name="BtnAddTask"
                Content="➕"
                Style="{StaticResource FABButtonStyle}"
                Grid.RowSpan="3"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="20"
                Click="BtnAddTask_Click"/>
    </Grid>
</UserControl>
